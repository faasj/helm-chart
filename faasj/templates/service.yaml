apiVersion: v1
kind: Service
metadata:
  name: {{ include "faasj.fullname" . }}
  labels:
    {{- include "faasj.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "faasj.selectorLabels" . | nindent 4 }}
